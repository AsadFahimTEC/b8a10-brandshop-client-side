
const Details = () => {
    // const {details, setDetails} = useState(null);
    // const {id} = useParams();
    // const detail = useLoaderData();
    // console.log(detail);
    return (
        <div>
            
        </div>
    );
};

export default Details;